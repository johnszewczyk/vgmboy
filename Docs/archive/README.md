# Archived Documents

These files preserve historical reports, analyses, and implementation handoffs. They are not
current project routing or implementation authority. Current behavior and engineering constraints
live under `ai/subsystem-human/` and `ai/subsystem-agent/`.
