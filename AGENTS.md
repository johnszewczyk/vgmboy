# AGENTS

Read this file, then `ai/project-info.md`, then the narrow note named by the task.

This package is the read-only catalog boundary shared by the macOS frontends.
Keep catalog writes and scanning in ScanSong, playback in VGMBoy, and
platform rendering in each consuming application.
